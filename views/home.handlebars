<!-- Site Navigation [START] -->
<nav class="navbar navbar-expand-lg navbar-dark primary-color">
  <!-- Site Navbar Brand -->
  <a class="navbar-brand" href="/">
    <h1>Mongo Scraper</h1>
  </a>
  <!-- Site Collapse Button -->
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#siteNavbar"
    aria-controls="siteNavbar" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <!-- Site Collapsible Content -->
  <div class="collapse navbar-collapse" id="siteNavbar">
    <!-- Site Navbar Links -->
    <ul class="navbar-nav mr-auto">
      {{!-- Site Navbar [Home Route] --}}
      <li class="nav-item active">
        <a class="nav-link" href="/">Home
          <span class="sr-only">(current)</span>
        </a>
      </li>
      {{!-- Site Navbar [Saved Articles] --}}
      <li class="nav-item">
        <a class="nav-link" href="/">Saved Articles</a>
      </li>

      {{!-- Site Navbar [Scrape New Articles!] Button --}}
      <a href="/scrape" id="btnScrape" type="button" class="btn btn-info btn-md my-2 my-sm-0 ml-3 font-weight-bold">
        Scrape New Articles!
      </a>
      {{!-- Site Navbar [Clear Articles!] Button --}}
      <a type="button" id="btnClear" class="btn btn-danger btn-md my-2 my-sm-0 ml-3 font-weight-bold">
        Clear Articles!
      </a>
    </ul>
  </div>
</nav>
{{!-- Site Navigation [End] --}}

{{!-- Site [Main Background Image] --}}
<div class="jumbotron text-center">
  {{!-- Site [Jumbotron Background] --}}
  <div class="bg">
  </div>
  {{!-- Site [Jumbotron Background Text] --}}
  <div class="caption">
    <h1 class="display-2 font-weight-bold">Mongo Scraper</h1>
    <p id="jumbo-describe" class="lead">New York Times Edition</p>
  </div>
</div>

{{!-- Site [Article Scrape Population Conditions] --}}
<div class="container-fluid">
  {{!-- Site [If the Articles Array Contains Data] --}}
  {{#if Article.length}}
  {{!-- Site [Loop Through Each Article's Title and Comment] --}}
  {{#each Article}}
  {{!-- Site [Article Scrape Results] --}}
  <div class="row">
    <div class="col-lg-12 col-sm-12">
      <div class="card mb-4">
        {{!-- Site [Article Title] --}}
        <div class="card-header primary-color white-text">
          <h4 class="card-title pt-2">{{this.title}}</h4>
        </div>
        {{!-- Site [Article Description / Comment] --}}
        <div class="card-body">
          <h4 class="card-title">{{this.comment}}</h4>
        </div>
      </div>
    </div>
  </div>
  {{/each}}

  {{!-- Site [If The Results Array Returns Empty, Show Placeholder] --}}
  {{else}}
  {{!-- Site [No New Articles] --}}
  <div class="alert alert-warning text-white" role="alert">
    <p class="h5 text-center"><b>Uh Oh.</b> Looks Like We Don't Have Any New Articles.</p>
  </div>
  {{/if}}
</div>